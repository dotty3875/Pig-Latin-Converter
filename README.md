Simple pig latin converter, does not include y for the sake of simplicy (and was out of scope for the project requirements).

Takes from inputWords.txt and outputs in terminal and into outputWords.txt.
