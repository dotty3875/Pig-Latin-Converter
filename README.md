Simple pig latin converter, does not include "y" as a vowel for the sake of simplicity (and was out of scope for the project requirements).

Takes input from inputWords.txt and prints in terminal and outputs to outputWords.txt.
